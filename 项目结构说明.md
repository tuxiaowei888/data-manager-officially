# 项目结构说明

## 📁 目录结构

```
数维数据管家系统/
├── 📄 main.py                      # FastAPI 主应用（入口）
├── 📄 requirements.txt             # Python 依赖
│
├── 📂 api/                         # API 路由
│   ├── auth.py                    # 用户认证 API
│   ├── channel.py                 # 渠道管理 API
│   ├── rules.py                   # 规则管理 API
│   ├── evaluation.py              # 评估引擎 API
│   ├── knowledge.py               # 知识库 API
│   └── reports.py                 # 报告管理 API
│
├── 📂 models/                      # 数据模型（ORM）
│   ├── user.py                    # 用户模型
│   ├── channel.py                 # 渠道模型
│   ├── rule_config.py             # 规则配置模型
│   ├── evaluation_result.py       # 评估结果模型
│   └── knowledge_doc.py           # 知识文档模型
│
├── 📂 schemas/                     # 数据 Schema（Pydantic）
│   ├── auth.py                    # 认证相关 Schema
│   └── channel.py                 # 渠道相关 Schema
│
├── 📂 config/                      # 配置文件
│   └── database.py                # 数据库配置
│
├── 📂 services/                    # 业务逻辑层
│   ├── evaluation.py              # 评估服务
│   ├── rule_calculator.py         # 规则计算
│   ├── report_generator.py        # 报告生成
│   └── policy_retriever.py        # 策略检索
│
├── 📂 frontend/                    # 前端页面
│   ├── index.html                 # 首页
│   ├── form.js                    # 表单逻辑
│   ├── report.html                # 报告页面
│   └── scoring-engine.js          # 评分引擎
│
├── 📂 scripts/                     # 工具脚本
│   ├── 备份数据库.bat              # 数据库备份脚本
│   └── init_db.py                 # 数据库初始化
│
├── 📂 database/                    # 数据库相关
│   └── migrations/                # 数据库迁移脚本
│       └── 002_add_users_and_channels.sql
│
├── 📂 data/                        # 数据文件
│   └── seed_rules_v1.json         # 规则种子数据
│
├── 📂 tests/                       # 测试文件
│   ├── quick_test.py              # 快速测试
│   └── test_api.py                # API 测试
│
└── 📂 docs/                        # 文档（可选创建）
```

## 🚀 快速开始

### 1. 启动服务

**方法一：双击批处理文件**
```
双击：启动服务.bat
```

**方法二：命令行**
```bash
python -m uvicorn main:app --host 0.0.0.0 --port 8000
```

### 2. 访问系统

- **前端页面**: http://localhost:8000
- **API 文档（中文）**: http://localhost:8000/api-docs-cn
- **Swagger 文档**: http://localhost:8000/docs
- **健康检查**: http://localhost:8000/health

## 🔧 常用操作

### 数据库备份

```bash
# 方法一：使用脚本
双击：scripts/备份数据库.bat

# 方法二：手动备份
docker exec numwei-mysql-1 mysqldump -u root -pnumwei2024 shuwei_data > backup.sql
```

### 数据库恢复

```bash
docker exec -i numwei-mysql-1 mysql -u root -pnumwei2024 shuwei_data < backup.sql
```

### 查看数据库

```bash
# 进入 MySQL 容器
docker exec -it numwei-mysql-1 mysql -u root -pnumwei2024

# 使用数据库
use shuwei_data;

# 查看所有表
show tables;

# 查看用户
select * from users;

# 查看渠道
select * from channels;
```

## 📊 API 分类

### 用户认证（4 个）
- POST `/api/v1/auth/register` - 用户注册
- POST `/api/v1/auth/login` - 用户登录
- GET `/api/v1/auth/me` - 获取当前用户
- POST `/api/v1/auth/logout` - 用户登出

### 渠道管理（6 个）
- GET `/api/v1/channels` - 获取渠道列表
- GET `/api/v1/channels/{id}` - 获取渠道详情
- POST `/api/v1/channels` - 创建渠道
- PUT `/api/v1/channels/{id}` - 更新渠道
- DELETE `/api/v1/channels/{id}` - 删除渠道
- GET `/api/v1/channels/{id}/users` - 获取渠道用户

### 规则管理
- GET `/api/v1/rules` - 获取规则列表
- POST `/api/v1/rules` - 创建规则
- PUT `/api/v1/rules/{id}` - 更新规则
- DELETE `/api/v1/rules/{id}` - 删除规则

### 评估引擎
- POST `/api/v1/evaluation/submit` - 提交评估
- GET `/api/v1/evaluation/{id}` - 获取评估结果

### 知识库
- GET `/api/v1/knowledge` - 获取知识文档
- POST `/api/v1/knowledge` - 上传知识文档

### 报告管理
- GET `/api/v1/reports` - 获取报告列表
- GET `/api/v1/reports/{id}` - 获取报告详情
- DELETE `/api/v1/reports/{id}` - 删除报告

## 📝 测试数据

### 预置用户

| 用户名 | 密码 | 类型 | 说明 |
|-------|------|------|------|
| admin | admin123 | 管理员 | 系统管理员 |
| test_user | test123 | 普通用户 | 测试用户 |
| vip_user | vip123 | VIP 用户 | VIP 测试用户 |

### 预置渠道

| 渠道代码 | 渠道名称 | 说明 |
|---------|---------|------|
| OFFICIAL | 官方渠道 | 官方直接客户 |
| PARTNER_A | 合作伙伴 A | 合作伙伴 A |
| PARTNER_B | 合作伙伴 B | 合作伙伴 B |

## 🔐 认证说明

### 获取 Token

```bash
POST http://localhost:8000/api/v1/auth/login
Content-Type: application/x-www-form-urlencoded

username=admin&password=admin123
```

返回：
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "bearer",
  "expires_in": 3600
}
```

### 使用 Token

在请求头中添加：
```
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
```

## 📚 重要文档

- [README.md](README.md) - 项目说明
- [BRD 需求确认与补充.md](BRD 需求确认与补充.md) - 业务需求
- [PRD_数维数据管家.md](PRD_数维数据管家.md) - 产品需求
- [UI 设计规范.md](UI 设计规范.md) - UI/UX 规范
- [TESTING_GUIDE.md](TESTING_GUIDE.md) - 测试指南
- [使用指南.md](使用指南.md) - 使用手册

## ⚠️ 注意事项

1. **端口占用**: 确保 8000 端口未被占用
2. **Docker 运行**: 确保 Docker Desktop 正在运行
3. **数据库密码**: 生产环境请修改默认密码
4. **Token 有效期**: Token 有效期 60 分钟，过期需重新登录
5. **CORS 配置**: 开发环境允许所有来源，生产环境需限制

## 🆘 常见问题

### 服务启动失败
```bash
# 检查端口占用
netstat -ano | findstr :8000

# 杀死占用端口的进程
taskkill /F /PID <进程 ID>
```

### 数据库连接失败
```bash
# 检查 Docker
docker ps

# 检查 MySQL 容器
docker ps | grep mysql

# 启动容器
docker start numwei-mysql-1
```

### 模块导入错误
```bash
# 重新安装依赖
pip install -r requirements.txt
```

---

**更新日期**: 2026-03-13
**版本**: V1.0 MVP
